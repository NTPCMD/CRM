{
  "name": "agencyos",
  "version": "0.0.0",
  "private": true,
  "description": "AgencyOS — the operating system for modern agencies.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:link": "supabase link --project-ref pzcckoinamekuxiqeqog",
    "db:push": "supabase db push",
    "db:seed": "supabase db execute --file supabase/seed.sql",
    "db:diff": "supabase db diff",
    "db:reset": "supabase db reset"
  },
  "devDependencies": {
    "supabase": "^2.0.0"
  }
}
